import Table from "tty-table";
export var ColumnAlignment;
(function (ColumnAlignment) {
    ColumnAlignment["Left"] = "left";
    ColumnAlignment["Center"] = "center";
})(ColumnAlignment || (ColumnAlignment = {}));
const tableOptions = {
    borderStyle: "solid",
    borderColor: "blue",
    headerAlign: "center",
    align: "left",
    color: "white"
};
export function generateOutputTable(columnAttributes, 
// eslint-disable-next-line
rowsData) {
    const tableHeader = columnAttributes.map((columnData) => {
        return {
            value: columnData.headerLabel,
            headerColor: "cyan",
            align: columnData.alignment ? columnData.alignment : ColumnAlignment.Left
        };
    });
    return Table(tableHeader, rowsData, tableOptions).render();
}
